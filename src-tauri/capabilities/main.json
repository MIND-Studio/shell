{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Least-privilege capability set for the main window. Grants ONLY what the shell + Vault frontend needs: the app's own crypto/auth commands (which by contract never return raw keys or plaintext), the deep-link callback for native OIDC, and opening the system browser for the auth flow. No fs, no shell-exec, no http allowlist, no clipboard-read.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "core:event:default",
    "core:window:default",
    "core:app:default",
    "deep-link:default",
    "opener:allow-open-url"
  ]
}
